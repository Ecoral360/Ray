Variables: { }

Add:
  ASSIGN: '@='

  PAREN_OPEN: '\('
  PAREN_CLOSE: '\)'

  datatypes:
    FLOAT: _?\d+\.\d+
    INT: _?\d+
    STRING: '".*?"'

  VARIABLE: '\b[a-zA-Z]\w*\b'
  FUNCTION: '([^@\s][\.:]?)|(`.*?`)'

Ignore:
  - '[\t ]+'

EndStatement: '\n'
